git clone でフォルダを作成

git clone https://github.com//daiki0708/My-first-git.git

同じことは sourceTree でも可能。
ファイル - 新規/クローンを作成する

---------------------------------------
フォルダの中で作業


ファイルを作成。編集。
---------------------------------------
(git bash)

$ git add .
$ git commit -m "メッセージ"
$ git push -u origin main

$git log (確認)

